import NotFound from '@/components/NotFound';

export default function NotFoundPage() {
  return (
    <NotFound
      title="Página no encontrada"
      message="Lo sentimos, la página que buscas no existe. Verifica la URL o usa los enlaces de navegación."
    />
  );
}